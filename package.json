{
  "name": "playwright",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "pretest": "rimraf allure-report/* && rimraf allure-results/*",
    "pregithub": "rimraf allure-report/* && rimraf allure-results/*",
    "github": "npx playwright test --reporter=line,allure-playwright",
    "postgithub": "allure generate allure-results -o allure-report --clean",
    "test": "npx playwright test --ui",
    "posttest": "allure generate allure-results -o allure-report --clean",
    "format": "prettier --write ."
  },
  "precommit": "format",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.32.3",
    "allure-commandline": "^2.21.0",
    "allure-playwright": "^2.1.0",
    "dotenv": "^16.0.3",
    "pengrape": "^1.3.3",
    "pre-commit": "^1.2.2",
    "prettier": "2.7.1",
    "rimraf": "^3.0.2"
  }
}
